name: Demo to create automatic PR
on:
  push:
    branches: [ "auto-pr-branch" ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checking out code   
        uses: actions/checkout@v3
        
      - name: set up java env
        uses: actions/setup-java@v1
        with:
          java-version: 11